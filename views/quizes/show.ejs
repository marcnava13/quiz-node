<form method="get" action="/quizes/<%= quiz.id %>/answer">
  <div class="form-group">
    <label for="pregunta"><%= quiz.pregunta %></label>
    <input type="text" class="form-control" id="pregunta" name="respuesta" placeholder="Responda aquí" required="required">
  </div>
  <input type="submit" class="btn btn-default" value="Enviar" />
</form>

<h3>Comentarios:</h3>
<% for(index in quiz.Comments){ %>
  <p> - <%= quiz.Comments[index].texto %></p>
<% } %>

<p><a class="btn btn-default" href="/quizes/<%= quiz.id %>/comments/new">Nuevo comentario</a></p>
